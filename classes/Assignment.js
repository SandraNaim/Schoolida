class Assignment{
    constructor (teacher, students, problems, file_upload, deadline, grade) {
        this.teacher = teacher;
        this.students = students;
        this.problems = problems;
        this.file_upload = file_upload;
        this.deadline = deadline;
        this.grade = grade;
    }

// not sure if we have to add these two functions


    /* getAssignment(){
        return `${this.teacher} ${this.students} ${this.problems} ${this.file_upload} ${this.deadline} ${this.grade}`

    }
    setAssignment(problems, teacher,students ,file_upload, deadline, grade){
      this.problems = problems
      this.teacher = teacher
      this.students = students
      this.file_upload = file_upload
      this.deadline = deadline
      this.grade = grade
  } */
}
